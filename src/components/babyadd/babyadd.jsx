import React from 'react';
import styles from './babyadd.module.css';

const BabyAdd = props => (
    <section className={styles.babyadd}>
    <h1>add Baby</h1>
    </section>
    );

export default BabyAdd;